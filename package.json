{
  "name": "tree-sitter-nim",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for nim",
  "main": "bindings/node",
  "scripts": {
    "test": "tree-sitter parse sample.nim"
  },
  "tree-sitter": [
    {
      "scope": "source.nim",
      "file-types": ["nim"]
    }
  ]
}
